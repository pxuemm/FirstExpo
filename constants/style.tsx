import { StyleSheet } from "react-native";

const MyStyle = StyleSheet.create({
    textTest : {
        color: "blue",
        fontSize: 15,
        fontWeight:"bold",
        marginTop: 20,
    }
})

export default MyStyle;